系统调用
	用户空间程序禁止直接执行kernel的代码或者操作内核数据。kernel必须提供一个机制--用户空间程序能够发送信号通知kernel他希望调用一个系统调用。
	应用程序通过机器寄存器通知kernel执行哪一个系统调用，带什么参数。系统调用通过数字标注，从0开始。例i386，如需请求系统调用5，那么用户空间程序必须在使用int指令前将5加到eax寄存器中。
	参数传递也通过类似方式进行处理。例i386，寄存器每一个可能的参数--ebx、ecx、edx、esi、edi顺序存储前5个参数。对于少见的超过5个参数的系统调用。实用一个寄存器指向用户空间中存储所有参数的缓存区即可。	
